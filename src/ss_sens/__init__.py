"""ss-sens — standalone edge IoT sensor mesh package.

Sub-packages:
  analytics/   Log analytics for ss-sens stack services (Mosquitto, ChirpStack, Frigate, OpenRemote).
  sensors/     ChirpStack LoRaWAN ingest and contract publishers.
  mesh/        Rolling sensor state and GPS-proximity mesh fusion.

Run the service with ``ss-sens serve`` or ``python -m ss_sens serve``.
"""
